# ─── Genel ───
NODE_ENV=development
PORT=3001

# ─── PostgreSQL ───
DATABASE_URL=postgresql://ringplanner:ringplanner_dev@localhost:5432/ringplanner?schema=public

# ─── Redis ───
REDIS_URL=redis://localhost:6379

# ─── JWT ───
JWT_SECRET=dev-secret-change-in-production-min-32-chars!!
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=dev-refresh-secret-change-in-production!!
JWT_REFRESH_EXPIRES_IN=7d

# ─── OSRM ───
OSRM_URL=http://localhost:5000

# ─── CORS ───
CORS_ORIGIN=http://localhost:3000

# ─── Cookie (production'da domain set et) ───
# COOKIE_DOMAIN=.ringplanner.com

# ─── Frontend ───
# NEXT_PUBLIC_API_URL=https://api.ringplanner.com/api/v1

# ─── Rate Limiting ───
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=100
